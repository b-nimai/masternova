# Prisma CLI reads this from the package root. Copy to .env for local work.
# Inside docker compose the host is the service name `postgres`; on the host, localhost.
DATABASE_URL="postgresql://masternova:masternova@localhost:5432/masternova?schema=public"
