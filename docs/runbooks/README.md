# Runbooks

**One file per alert**, created in the same commit as the alert it serves.

Shape: `symptom → likely cause → checks → fix → escalation`.

An alert without a runbook is a pager that wakes someone up with no next step.
